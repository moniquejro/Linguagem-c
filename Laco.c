#include <stdio.h>

int main ()
{
    int count;
    int num = 1;

    for (count = 1; count <= 10; count++) //++ incrementa +1 sempre
    {
        printf("teste %i.\n", count);
    }
    

    return 0;
}